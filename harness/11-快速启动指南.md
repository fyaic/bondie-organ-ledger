# 11-快速启动指南（30 秒极速版 / 会话恢复用）

## 你要做什么（一句话）
在 `C:\Hello-World\organledger` 上做**外科手术式跨平台加固**：修 2 个会破坏 Windows 哈希链的 BLOCKER + 3 个 MAJOR，加 CI 三矩阵，全程不重写、不碰「已做对」的代码。

## 5 个核心任务（照做即可）
1. 🔴 建 `.gitattributes`（`* text=auto eol=lf` + 二进制标注）→ `git add --renormalize .`
2. 🔴 `src/onboard/backfill.ts` 的 `split("\n")` → `split(/\r?\n/)`（+ grep 同类）
3. 🟡 `src/cli/index.ts`：`if(win) process.on("SIGBREAK", shutdown)`
4. 🟡 `test/hermes.test.ts`：python 探针，缺则 `skip`
5. 🟡 `.github/workflows/ci.yml`：加 `matrix.os: [ubuntu, windows, macos]`

## 铁律
- 每改一个 → `node --test test/*.test.ts` → 绿了再下一个
- 不改 `canonicalJson`/账本序列化、不改 `02矩阵C区`、不动 `autostart.ts`、不加新依赖
- 遇阻塞：记 TODO → 跳过 → 继续，**不问人类**

## 会话恢复三步
1. 读 `TODO.md` → 我在哪
2. 读 `99-执行偏差日志.md` → 已做什么 / 已知坑
3. 从 `09-执行时序与里程碑.md` 的当前 Phase 续跑

## 详情去哪查
决策 → `03`；file:line 修法 → `04`；CI → `05`；Mac验证 → `06`；检查项 → `10`
