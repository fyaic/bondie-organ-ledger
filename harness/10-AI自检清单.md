# 10-AI 自检清单

> 每个 Phase 完成后逐项打勾。用「是/否」可验证的项，不是「感觉 OK」。

## 全局神圣不变量（每次改完都自问）
- [ ] 我没有修改 `canonicalJson` 或任何账本记录的序列化字节
- [ ] 我没有改动 `02-差异分析矩阵.md` 的 C 区「已做对」清单里的代码
- [ ] 我没有改动 `autostart.ts`（⏳二期）
- [ ] 我没有 `npm install` 任何新包
- [ ] `node --test test/*.test.ts` 仍全绿（或与基线相同的 skip/pass 结构）

## P-1 环境探测
- [ ] node ≥ v24，git 可用
- [ ] `feat/cross-platform` 分支已建
- [ ] 绿色基线已跑并记录 pass 数到 99-偏差日志
- [ ] `.gitattributes` 确认不存在
- [ ] 已 grep 出 CRLF 风险点清单

## P1 .gitattributes（🔴）
- [ ] `.gitattributes` 已创建，含 `* text=auto eol=lf` 与二进制 `binary` 标注
- [ ] `git add --renormalize .` 无报错
- [ ] 重正规化后测试仍全绿
- [ ] png/ico 等二进制未被文本化（`git diff --stat` 里无二进制被改）

## P2 CRLF 解析（🔴）
- [ ] `backfill.ts` 的 `split("\n")` → `split(/\r?\n/)`
- [ ] grep 复查：所有「解析外部命令输出/磁盘文本」的 split 已改
- [ ] 「切割自己拼的 `\n` 字符串」的 split 保持不动（判断有依据）
- [ ] `test/attribution.test.ts test/provenance.test.ts test/core.test.ts` 全绿

## P3 SIGBREAK（🟡）
- [ ] `if (process.platform === "win32") process.on("SIGBREAK", shutdown)` 已加
- [ ] 已读 `daemon.ts` 确认 shutdown 幂等（或已加 guard）
- [ ] SIGTERM/SIGINT 注册保持不动

## P4 python 探测 + CI（🟡）
- [ ] hermes 测试：缺 python 走 `skip` 非 `fail`（已用 node:test skip 机制）
- [ ] 本机有 python 时该测试仍 pass
- [ ] `ci.yml` 有三平台 matrix + `runs-on: ${{ matrix.os }}`
- [ ] 原有 CI 步骤（checkout/setup-node/typecheck）全部保留
- [ ] 本机模拟 CI：`npm run typecheck && node --test test/*.test.ts` 绿

## P5 全量测试
- [ ] `node --test test/*.test.ts` 全绿
- [ ] 若有回归，已定位是哪个 Phase 引入并修复
- [ ] pass 数 ≥ 基线 pass 数（没有测试被“改没”）

## P6 收尾
- [ ] 任务5 MINOR 已做或已记 TODO 跳过（有理由）
- [ ] `06-Mac真机验证清单.md` 已校对（命令与真实 CLI 子命令名一致——先 `node src/cli/index.ts --help` 或读 cli/index.ts 的 switch 确认子命令名）
- [ ] 改动已 commit 到 `feat/cross-platform`（commit message 清晰）
- [ ] TODO 标注「CI 三矩阵待 push 验证」「Mac 真机待人类验证」

## 自检失败处理流程
```
某检查项 = 否
  → 是否核心不变量（全局区）被破坏？
      是 → 立即回退该改动（git checkout -- <file>），重做，绝不带病前进
      否 → 记 99-偏差日志 + TODO「已知阻塞」→ 尝试替代 → 仍不行则跳过 → 继续
```
