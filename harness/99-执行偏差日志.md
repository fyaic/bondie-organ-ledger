# 99-执行偏差日志

> 记录：绿色基线、每个决策偏差、阻塞、跨平台异常。人类事后审阅。

## 绿色基线（P-1 填）
```
日期/时间：2026-07-15 18:54 CST
执行平台：macOS (Darwin 25.5.0)  ← 注意：本次在 Mac 上执行，非计划书假设的 Windows
node 版本：v26.0.0（≥24 ✓）  git 2.53.0
测试基线命令：node --test test/*.test.ts
pass 数：86   skip 数：0   fail 数：4
判断：4 个 fail 非我改坏——是【既有的 macOS 专属 bug】，Windows 参谋长看不到。根因见「跨平台异常观察 #1」。
失败用例（全在 provenance.test.ts，均涉及嵌套 embedded 仓库检测）：
  - resolveSources: finds parent (rel='') + embedded repo as separate sources
  - inspectSource: remote/branch/dirty resolved; no-upstream ahead/behind null
  - backfill embedded repo: content tickets carry verified provenance
  - backfill --no-provenance opt-out: content tickets carry no provenance
CRLF 风险点 grep 结果（split("\n") 命中清单）：
- src/onboard/backfill.ts:190  body.split("\n")          → 🔴 主 BLOCKER，解析 git show 输出，必改
- src/onboard/init.ts:155      add.out.split("\n")[0]    → 🟡 解析 git 输出，改 /\r?\n/ 无害
- src/onboard/init.ts:189      commit.out.split("\n")[0] → 🟡 同上
- src/onboard/autostart.ts:40  (Error).message.split("\n")[0] → ⛔ 内存字符串+autostart 二期勿动，保持不动
- backfill.ts:393 已用 /\r?\n/（正确范式，不动）
```

## 偏差记录
| # | Phase | 偏差点 | 参谋长原计划 | 实际做法 | 原因 |
|---|-------|--------|-------------|---------|------|
| 1 | 后置A/P5 | macOS 嵌套仓库检测失效 | 计划无此项（Windows 审计看不到） | `provenance.ts:resolveSources` 里 `homeAbs` 改用 `fs.realpathSync` 规范化，使其与 `git rev-parse --show-toplevel` 的真实路径可比 | macOS `/var`、`/tmp` 符号链接导致 path.resolve 与 git 真实路径失配。修复后 provenance 4 测试转绿，也是 CI `macos-latest` 能绿的前提 |
| 2 | P4 | CI 文件不存在 | 计划假设 `.github/workflows/ci.yml` 仅 ubuntu、需升级为三矩阵 | 仓库根本无 `.github/`，直接**从零创建**三平台矩阵 ci.yml | 现场与审计快照不一致；完整实现模式下创建即达成目标 |
| 3 | P4 | CI 测试命令 | 计划纠结 Windows glob 展开 | 直接用 `npm test`（=`node --test test/*.test.ts`），Node24 的 --test 自带 glob 展开，三平台一致 | 无需 shell 展开，最稳 |

## 强制尝试记录（切换替代方案前必填）
```
任务：
开始时间：
尝试方案 1：            结果：
尝试方案 2：            结果：
尝试方案 3：            结果：
决策：
```

## 跨平台异常观察
> 任何 Win 特有的报错、路径异常、测试差异记这里。
| # | 现象 | 文件 | 是否修复 |
|---|------|------|---------|
| 1 | **【新发现·macOS 专属 BLOCKER】** `resolveSources` 用 `path.resolve(home)`（不解析符号链接，得 `/var/...`）与 `git rev-parse --show-toplevel`（返回真实路径 `/private/var/...`）比较，macOS 上 `/var`、`/tmp` 均为指向 `/private` 的符号链接 → `homeAbs !== parentRoot`、`childRoot !== childAbs` 全部失配 → 嵌套 embedded 仓库检测失效 → provenance 4 测试挂。Windows/Linux 的 tmpdir 无此符号链接故参谋长审计（Windows）看不到。这正是「Mac 真机验证」要抓的问题。 | `src/onboard/provenance.ts:53 resolveSources`（`revParseToplevel`/`homeAbs`） | ✅ 见偏差记录 #1（后置轮 A 修复：`homeAbs` 用 `fs.realpathSync` 规范化） |

## 会话交接
> 上下文重置前，在此写一句「当前状态 + 下一步」，供恢复。
> 交接：（空）
